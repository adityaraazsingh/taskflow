-- liquibase formatted sql

-- changeset adity:20260817-3 splitStatements:false
CREATE TABLE ${schemaName}.ACTIVITY_ENTITY (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    CREATED_AT TIMESTAMP,
    DATA VARCHAR(255),
    EVENT_NAME VARCHAR(50),
    PROJECT_ID BIGINT,
    TASK_ID BIGINT,
    TENANT_NAME VARCHAR(255),
    CONSTRAINT PK_ACTIVITY PRIMARY KEY (ID)
);

-- changeset adity:20260817-4 splitStatements:false
ALTER TABLE ${schemaName}.ACTIVITY_ENTITY ADD COLUMN STATUS VARCHAR(50);
