package com.projectManagement.taskflow.exception;

enum ErrorCode {
    VALIDATION_ERROR,
    TASK_NOT_FOUND,
    UNAUTHORIZED,
    INTERNAL_ERROR,
    INVALID_ARGUMENT,
    USER_NOT_FOUND,
    PROJECT_NOT_FOUND,
    ACCESS_DENIED,
    INVALID_CREDENTIALS
}
